/**
 * Configuración modificable al desplegar, sin recompilar Angular.
 *
 * Una cadena vacía utiliza `/api` sobre el mismo host de la aplicación.
 * Si la API vive en otro origen, indicar la base completa incluyendo `/api`,
 * por ejemplo: `https://api.ejemplo.com/api`.
 */
window.__IRS_CONFIG__ = {
  apiBaseUrl: '',
};
